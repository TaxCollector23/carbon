{
  "name": "@carbon/desktop",
  "version": "0.2.0",
  "private": true,
  "description": "Carbon desktop — run stateful API replicas from a native window.",
  "scripts": {
    "dev": "tauri dev",
    "prepare:sidecar": "bash scripts/prepare-sidecar.sh",
    "build": "pnpm prepare:sidecar && tauri build",
    "check": "cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
