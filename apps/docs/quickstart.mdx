---
title: 'Quickstart'
description: 'From npm install to a stateful replica in under five minutes.'
---

## Or start from a sample

Not ready to point Carbon at your own spec yet? Every install ships with a
curated set of real API subsets you can spin up in one click.

**From the dashboard** — open the workspace overview and hit **Try a sample**.
Pick one, and Carbon creates a fresh project, ingests the bundled OpenAPI
spec, and drops you on the projects list.

**From the CLI**:

```bash
carbon samples list
carbon samples instantiate stripe
```

Available sample ids: `petstore`, `stripe`, `github`, `shopify`, `openai`,
`twilio`. Each is a 30–50 endpoint subset of the real API — small enough to
ingest instantly, large enough to feel like the real thing.

**From the public gallery** — [carbon.dev/gallery](https://carbon.dev/gallery)
links each entry into the dashboard via `?sample=<id>`, which triggers the
same one-click flow while signed in.

---

## 1. Install

The CLI publishes to npm as **`carbon-dev`** and installs a `carbon`
executable (with `carbon-dev` and `carbon-api` as aliases).

```bash
npm install -g carbon-dev
```

Or without a global install:

```bash
npx carbon-dev <command>
```

## 2. Initialize a project

```bash
mkdir petstore && cd petstore
carbon init --name petstore
```

That writes a `carbon.config.ts` you can edit later — project name, slug,
runtime port. Re-running `carbon init` in the same directory prompts before
overwriting (use `--force` in scripts).

## 3. Ingest a spec

Any supported description works — OpenAPI, AsyncAPI, GraphQL SDL, protobuf,
HAR, or a Postman collection. Carbon parses it into its Intermediate
Representation and prints a summary.

```bash
carbon ingest ./petstore.openapi.json
```

```
Parsed petstore v1.0.0
Endpoints     14
Resources     3
Relationships 2
```

## 4. Boot the runtime

```bash
carbon emulate --from ./petstore.openapi.json --port 4000
```

The replica listens on `http://localhost:4000`. Health, inspection, and
state routes live under `/__carbon/*`. Pass `--watch` to auto-reload on
spec changes.

## 5. Hit it like a real API

```bash
curl -X POST http://localhost:4000/pets \
  -H 'content-type: application/json' \
  -d '{"name":"Milo","tag":"cat"}'

curl http://localhost:4000/pets
# → the pet you just created is in the list
```

State survives across requests because Carbon compiled the spec into a
behavior graph — it isn't returning canned JSON.

## 6. Snapshot the state

```bash
carbon snapshot save baseline
```

Snapshots land under `.carbon/snapshots/` as JSON. Restore any time:

```bash
carbon snapshot load baseline
```

That's the whole loop: **ingest → emulate → mutate → snapshot → restore**.

<Note>
  **Try the examples.** Runnable copy-paste samples live under
  [`examples/`](https://github.com/carbondev/carbon/tree/master/examples) in the
  repo:

  - [`examples/sdk-basic`](https://github.com/carbondev/carbon/tree/master/examples/sdk-basic)
    — boot a replica from an OpenAPI file with `carbon.emulate()`.
  - [`examples/sdk-with-ai`](https://github.com/carbondev/carbon/tree/master/examples/sdk-with-ai)
    — same, plus AI-inferred resources gated by `replica.assertQuality()`.
  - [`examples/sdk-vitest`](https://github.com/carbondev/carbon/tree/master/examples/sdk-vitest)
    — drop the SDK into a Vitest suite with proper setup/teardown.

  From a clone: `pnpm --filter @carbon/example-sdk-basic dev`.
</Note>

## Next steps

- [Spec to runtime](/concepts/spec-to-runtime) — how ingest, IR, and the
  behavior graph fit together.
- [State engine](/concepts/state-engine) — snapshots, journal, rewind.
- [Chaos](/concepts/chaos) — latency and error injection for resilience
  tests.
- [CLI reference](/cli/reference) — every command and flag.
