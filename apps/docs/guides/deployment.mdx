---
title: 'Deployment'
description: 'Where the runtime lives in production-adjacent environments.'
---

Carbon is designed to run on developer machines and in CI. It is not a
production API server. That said, a shared internal replica can be useful for
review and QA. The runtime is a plain Node process behind any reverse proxy.
