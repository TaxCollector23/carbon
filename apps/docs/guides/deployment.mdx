---
title: 'Deployment'
description: 'Where the runtime lives in production-adjacent environments.'
---

Carbon is designed to run on developer machines and in CI. It is not a
production API server. That said, several teams host a shared internal
replica for demos and QA — the runtime is a plain Node process behind any
reverse proxy.
