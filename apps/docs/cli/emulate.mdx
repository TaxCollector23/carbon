---
title: 'carbon emulate'
description: 'Run a deterministic replica.'
---

```bash
carbon emulate --from ./stripe.json --port 8787
```

Point your app's base URL to `http://localhost:8787`. Requests are served by the
compiled behavior graph and state engine, keeping upstream providers out of the
test path.
