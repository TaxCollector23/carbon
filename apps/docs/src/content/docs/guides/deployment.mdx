---
title: 'Deployment'
description: 'Where to run replicas in production-adjacent environments.'
---

Carbon is designed for developer machines, CI, and shared internal replicas. It
is not intended to replace a production API server. For review and QA, run the
runtime as a plain Node process behind any reverse proxy.
