---
title: 'Snapshots'
description: 'Save and restore full-state snapshots.'
---

```ts
const snap = await replica.snapshot.save('after-refund');
// Commit snap to your repo; it is stable JSON.
await replica.snapshot.restore(snap);
```
